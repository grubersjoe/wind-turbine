{
  "name": "wind-turbine",
  "author": "Jonathan Gruber <gruberjonathan@gmail.com>",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "check": "prettier --check --plugin=@prettier/plugin-xml --xml-whitespace-sensitivity=ignore .",
    "dev": "vite",
    "format": "prettier --write --plugin=@prettier/plugin-xml --xml-whitespace-sensitivity=ignore .",
    "preview": "vite build && vite preview"
  },
  "devDependencies": {
    "@prettier/plugin-xml": "^3.4.2",
    "prettier": "^3.6.2",
    "vite": "^7.1.0"
  }
}
